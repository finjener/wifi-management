# WiFi Manager Configuration
# This file is sourced by wifi-manager.sh
# User overrides can be placed in ~/.config/wifi-manager/config.env

# -----------------------------------------------------------------------------
# Directory Paths
# -----------------------------------------------------------------------------

# Base directory for data files (relative to script location or absolute)
WIFI_MANAGER_BASE="${WIFI_MANAGER_BASE:-data}"

# System NetworkManager connections directory
SYSTEM_DIR="${SYSTEM_DIR:-/etc/NetworkManager/system-connections}"

# Local connections directory (relative to BASE_DIR)
LOCAL_DIR_NAME="${LOCAL_DIR_NAME:-network_connections}"

# Android WifiConfigStore.xml filename
XML_FILENAME="${XML_FILENAME:-WifiConfigStore.xml}"

# Networks report filename
REPORT_FILENAME="${REPORT_FILENAME:-networks_report.md}"

# -----------------------------------------------------------------------------
# ADB Configuration
# -----------------------------------------------------------------------------

# Android WifiConfigStore path (Android 10+)
ADB_WIFI_PATH="${ADB_WIFI_PATH:-/data/misc/apexdata/com.android.wifi/WifiConfigStore.xml}"

# Legacy Android WifiConfigStore path (Android 9 and below)
ADB_WIFI_PATH_OLD="${ADB_WIFI_PATH_OLD:-/data/misc/wifi/WifiConfigStore.xml}"

# Temporary file location on Android device for transfers
ADB_TEMP_PATH="${ADB_TEMP_PATH:-/sdcard/WifiConfigStore.xml.tmp}"

# -----------------------------------------------------------------------------
# Display Settings
# -----------------------------------------------------------------------------

# Enable colored output (set to "false" to disable)
COLOR_OUTPUT="${COLOR_OUTPUT:-true}"

# Verbose logging (set to "true" for debug output)
VERBOSE="${VERBOSE:-false}"
